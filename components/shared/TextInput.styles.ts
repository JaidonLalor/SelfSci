import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textInput: {
        minHeight: 40,
        backgroundColor: 'white',
        borderRadius: 8,
        paddingHorizontal: 8,
        paddingVertical: 10,
        fontSize: 16,
        color: '#7E7E7E',
    }
})